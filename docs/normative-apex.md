# Normative Apex

This doctrine governs the **structural** face of source of intent: what a source of intent *is* — the locatable governing role that supplies or authorizes purpose and the governing standard, and from which legitimate authority, closure, and recourse take their bearings. The **operational** face — how purpose is recovered, validated, preserved, and routed across surfaces — lives in [`docs/source-of-intent.md`](source-of-intent.md). This doc is the apex the recursion stands on; that doc is how the apex is handled in practice.

## The apex

The governing standard a system runs on must be **supplied to the execution function; it cannot be legitimated merely by executing it.** A model, an orchestration layer, a decentralization scheme, a clean data foundation, a perfected hardware interlock — none supplies it. It enters from a locatable governing role.

The same person or institution may occupy both the source role and the execution role. **The dependency is logical, not necessarily a separation of actors:** what cannot happen is the execution function authorizing the basic norm it presupposes, regardless of who runs it.

Source of intent is a **locatable governing role, not exhaustive specification or execution control.** Execution may be delegated, stochastic, collective, or exploratory; the apex remains attributable as long as purpose, authorization, closure, and answerability remain attributable rather than dissolving into aggregation. The inverse error is to let private intention float free of those gates.

## The decomposition: intent · authority · recourse

Accountability decomposes into three things distribution can never produce:

- **Intent** — *before* the act: what the system is for and which governing standard applies.
- **Authority** — the *right* to act, not merely the power.
- **Recourse** — *after* the act: who answers when an authorized move is later judged wrong.

The three are jointly necessary. **Intent without authority is preference. Authority without a nameable intent is power. Either without recourse is unaccountable.** The source may be an individual, office, institution, governed collective, constitutional order, or delegated authority — but purpose, authorization, closure, and answerability must remain locatable. It need not precompute every result; it governs why the aperture exists, where its bounds sit, who may accept, reject, revise, or bind what emerges, and who answers for the result.

## The field models a dyad; the structure is a stack

The debate usually runs on a two-term ontology — human vs. machine, two minds across a gap. That interactional dyad is real, but it sits **inside a governance stack**, and the executor may be **human or computational**. The dyad asks who is across the gap; the apex asks **which layer answers.** "Apex" names **structural dependency and answerability, not moral status or dominance** — a governed stack has different heights, but that is ordering, not rank.

The requirement holds **independent of the consciousness question.** Whether or not anyone is home in the executor, a consequential system still needs a locatable apex. The requirement is over-determined: the purpose and beneficiary of the system, *and* the executor's incapacity to legitimate its own standard, each suffice to secure it.

## The non-identities

The subject is one primitive stated in distinctions the discourse keeps eliding:

- **power ≠ authority** — the capacity to act is not the right to act.
- **aggregation ≠ origination of intent** — a blend of participants, or a statistical composite of preferences, does not originate a governing intent. A poll aggregates; a constituted collective decides.
- **intention alone ≠ legitimate authority** — private will does not self-authorize; it acts through attributable authority, closure, and recourse.
- **the interactional dyad ≠ the governance stack that contains it** — the field mistakes the two-minds-across-a-gap picture for the whole structure.

Two further distinctions are argued in full in the essay line and carried here by reference: the **residue of judgment ≠ the act of judgment**, and **outputs ≠ intent** (see *Judgment Doesn't Transfer*).

## The operational floor is downstream of the standard

The normative apex supplies or authorizes the governing standard. Governance constitutes or recognizes authority and translates the standard into rules, thresholds, bounded decision rights, verification, adjudication + recourse. The **operational floor** is the boundary those rules establish. A substrate or execution mechanism may carry, expose, clear, or enforce that floor; it cannot legitimate the standard that authorized it.

Every substrate verb — clear, see, enforce, run, audit, arrest, de-energize — operates on a floor whose location has already been supplied to that substrate. ***Define* is never on the substrate's list.** The standard is presupposed, supplied upstream, external. Four structurally different worked cases make the point and are argued in full in the essay line: the **architecture** layer, the **state / enforcement** layer (*Authority Without a Standard*), the **operational** layer (detection supplies what is true, never what the truth is allowed to mean), and the **physical / limit** layer (*Determinism Is Not a Standard* — perfecting the floor only throws the standard's absence into sharper relief). *The Floor Is Not the Standard* synthesizes all seven essay-arc frames; *Judgment Doesn't Transfer* argues the corpus frame.

## Relation to governance

The apex states the normative **requirements** — purpose, governing standard, legitimate authority, closure, recourse. **Governance** ([`docs/governance.md`](governance.md)) provides their institutional **mechanisms** — how authority is constituted or recognized, how rules are made and applied, how application is verified and adjudicated, how recourse is operated. Authority and recourse appear in both docs for this reason: the apex names them as requirements; governance operates them as mechanisms. The two are **jointly necessary in consequential, delegated, or collective systems**, and are articulated separately rather than merged.

## Jurisprudential anchors

- **Kelsen — the Grundnorm.** The basic norm is presupposed, supplied to the system, not generated by it.
- **Dworkin — weak vs strong discretion.** Bounded interpretive latitude inside a standard vs the authority to set the standard.
- **Fuller — the Speluncean Explorers.** Competing legitimate sources of authority; the standard is contested, never self-evident from the facts.
- **Weiner — the Declaration of Intent.** Intent declared and binding ahead of the act.
- **Power vs right.** The load-bearing distinction throughout.

## Classification

The structural face of source of intent, generalized beyond any single project. It clears the promotion bar by recurrence: the same apex absence recurs across distribution, data, enforcement, supply, operations, silicon, and corpus framings (the essay arc), and across image-making, asset production, and civic domains. It is the apex the operational handling doctrine ([`docs/source-of-intent.md`](source-of-intent.md)) presupposes.

## Self-superseding clause

This doctrine should be superseded by:

- a thinner apex statement if the non-identity field earns its own home (e.g. `docs/non-identities.md`);
- a refactor if `docs/governance.md` and this doc are found to share enough structure to warrant a common parent — the "co-primitive" question is deliberately held open here, not asserted.

The doctrine is not finished; it states the apex modestly and refers the full arguments to the essay line.

## Anchors

Anchor reading lives in [`docs/articles.md`](articles.md). The source-of-intent arc and the trilogy — *Determinism Is Not a Standard*, *Judgment Doesn't Transfer*, *The Floor Is Not the Standard* — carry the full arguments this doctrine states in compact form.
